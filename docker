docker run -d -p 8080:8080 mongo
docker run d= background  p = port  image name:version


MONGO_INITDB_ROOT_USERNAME =
MONGO_INITDB_ROOT_PASSWORD


--name is any name of iamge -p is port  -e vars  -d download this image followed by version 
docker run --name sequel_my -p 3306:3306 -e MYSQL_ROOT_PASSWORD=password -d mysql
